# Lefthook configuration
# https://lefthook.dev/

pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: bun run check:ultracite -- {staged_files}
      stage_fixed: true
