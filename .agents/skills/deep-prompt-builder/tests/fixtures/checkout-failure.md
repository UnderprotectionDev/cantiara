# Checkout Failure

The checkout provider may accept a charge even when the client loses the success response.
The incident report records repeated customer charges after the client retried an operation
whose outcome was unknown.
