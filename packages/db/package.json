{
  "name": "@cantiara/db",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./src/index.ts"
    },
    "./*": {
      "types": "./dist/src/*.d.ts",
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "check-types": "tsc -b",
    "db:generate": "bun --env-file=../../apps/server/.env.local ./node_modules/.bin/drizzle-kit generate",
    "db:migrate": "bun --env-file=../../apps/server/.env.local ./scripts/migrate.ts",
    "db:migrate:deploy": "bun ./scripts/migrate.ts",
    "db:push": "bun --env-file=../../apps/server/.env.local ./node_modules/.bin/drizzle-kit push",
    "db:security:generate": "bun --env-file=../../apps/server/.env.local ./node_modules/.bin/drizzle-kit generate --config drizzle.security.config.ts",
    "db:security:migrate": "bun --env-file=../../apps/server/.env.local ./scripts/migrate.ts --security-events",
    "db:security:migrate:deploy": "bun ./scripts/migrate.ts --security-events",
    "db:studio": "bun --env-file=../../apps/server/.env.local ./node_modules/.bin/drizzle-kit studio"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@t3-oss/env-core": "catalog:",
    "drizzle-orm": "^0.45.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cantiara/config": "workspace:*",
    "drizzle-kit": "^0.31.10",
    "typescript": "catalog:"
  }
}
